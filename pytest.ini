[pytest]
addopts = -q --cache-clear
cache_dir = .pytest_cache

# Exclude temp/output directories from collection to avoid Windows lock issues.
norecursedirs =
    out
    _tmp
    .pytest_cache
    .pytest_tmp
    .tmp_pytest*
    pytest-cache-files-*

testpaths =
    tests